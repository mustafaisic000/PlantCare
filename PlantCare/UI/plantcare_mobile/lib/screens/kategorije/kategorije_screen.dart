import 'package:flutter/material.dart';

class KategorijeScreen extends StatelessWidget {
  const KategorijeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Kategorije', style: TextStyle(fontSize: 24)),
    );
  }
}
