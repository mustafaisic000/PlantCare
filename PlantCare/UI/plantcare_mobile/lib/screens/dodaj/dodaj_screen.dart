import 'package:flutter/material.dart';

class DodajScreen extends StatelessWidget {
  const DodajScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Dodavanje sadržaja', style: TextStyle(fontSize: 24)),
    );
  }
}
